apiVersion: v1
kind: Service
metadata:
  name: bouncy-api-service
  labels:
    app: bouncy-api
spec:
  selector:
    app: bouncy-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
